{
  "buildCommand": "cd api && npx prisma generate && cd .. && npm run --workspace=web build",
  "outputDirectory": "web/dist",
  "installCommand": "npm ci",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
